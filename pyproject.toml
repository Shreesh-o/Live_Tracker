[project]
name = "live-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.15.1",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
live-tracker = "live_tracker:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
